# ===========================================
# CHARLIE APP - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values

# ------------------------------------------
# SUPABASE
# ------------------------------------------
# Your Supabase project URL (Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Service role key - KEEP SECRET, server-side only (Settings > API > service_role)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ------------------------------------------
# NEWS REFRESH
# ------------------------------------------
# Secret token to protect the news refresh endpoint (min 16 chars)
# Generate with: openssl rand -hex 32
NEWS_REFRESH_SECRET=your-secret-token-min-16-chars

# ------------------------------------------
# MARKET NEWS PROVIDER
# ------------------------------------------
# Choose your news provider: "marketaux" or "finnhub"
MARKET_NEWS_PROVIDER=marketaux

# MarketAux API key (required if MARKET_NEWS_PROVIDER=marketaux)
# Get it at: https://www.marketaux.com/
MARKETAUX_API_KEY=your-marketaux-api-key

# Finnhub API key (required if MARKET_NEWS_PROVIDER=finnhub)
# Get it at: https://finnhub.io/
FINNHUB_API_KEY=your-finnhub-api-key
